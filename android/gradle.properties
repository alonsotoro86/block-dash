org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# Kotlin's incremental compiler can't compute a relative path when the Pub
# cache (C:\Users\...) and this project (D:\PROYECTOS\...) live on different
# drive letters on Windows, which crashes with "different roots". Disabling
# incremental compilation avoids that relative-path computation entirely.
kotlin.incremental=false
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false
